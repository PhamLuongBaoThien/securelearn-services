import crypto from 'crypto'; import { Types } from 'mongoose'; import { RoutingKey, type InboxEventType } from '@securelearn/common'; import { Ticket, TICKET_TYPES, TICKET_STATUSES } from '../models/ticket.model'; import { TicketMessage } from '../models/ticketMessage.model'; import { TicketActivity } from '../models/ticketActivity.model'; import { TicketAttachment } from '../models/ticketAttachment.model'; import { identityGrpcClient, courseGrpcClient } from '../config/grpc'; import { enqueueEvent } from './outbox.service'; import { storeFile, getFile } from './storage.service';
type Actor={id:string;type:'USER'|'ADMIN';role:string;name?:string;email?:string};
const eventKey:Record<string,RoutingKey>={REPORT:RoutingKey.REPORT_CREATED,SUPPORT:RoutingKey.SUPPORT_REQUEST_CREATED,FEEDBACK:RoutingKey.FEEDBACK_CREATED};
class TicketService{
 private async snapshot(actor:Actor){const row=await identityGrpcClient.getIdentitySnapshot({identityId:actor.id,identityType:actor.type});if(!row.found||!row.active)throw new Error('Tài khoản không tồn tại hoặc không hoạt động.');return{id:row.identityId,name:row.fullName,email:row.email,role:row.role,permissions:row.permissions};}
 private async target(input:any){if(input.type==='USER'){const r=await identityGrpcClient.getIdentitySnapshot({identityId:String(input.id||''),identityType:'USER'});if(!r.found)return null;return{type:'USER',id:r.identityId,title:r.fullName,courseId:'',ownerUserId:r.identityId,actionUrl:`/users/${r.identityId}`};}return courseGrpcClient.getReportTargetSnapshot({targetType:String(input.type||''),targetId:String(input.id||''),parentCourseId:String(input.courseId||'')});}
 private eventPayload(ticket:any,actor:Actor,extra:Record<string,unknown>={}){return{eventId:crypto.randomUUID(),ticketId:ticket.id,resourceId:ticket.id,type:ticket.type as InboxEventType,title:ticket.title,summary:ticket.description.slice(0,240),senderId:ticket.sender.id,senderName:ticket.sender.name,senderEmail:ticket.sender.email,senderRole:ticket.sender.role,assignedAdminId:ticket.assignedAdmin?.id||'',actorId:actor.id,actorType:actor.type,status:ticket.status,createdAt:new Date().toISOString(),occurredAt:new Date().toISOString(),actionUrl:actor.type==='USER'?`/admin/notifications/inbox?id=${ticket.id}`:`/support/tickets/${ticket.id}`,...extra};}
 async create(actor:Actor,input:any){if(!TICKET_TYPES.includes(input.type)||!String(input.title||'').trim()||!String(input.description||'').trim())throw new Error('Loại, tiêu đề và nội dung là bắt buộc.');const sender=await this.snapshot(actor);let target=null;if(input.type==='REPORT'){if(!input.target?.type||!input.target?.id)throw new Error('Báo cáo phải có đối tượng.');target=await this.target(input.target);if(!(target as any)?.found&&input.target.type!=='USER')throw new Error('Đối tượng báo cáo không tồn tại hoặc không khớp.');if(!target)throw new Error('Đối tượng báo cáo không tồn tại.');}const ticket=await Ticket.create({type:input.type,title:String(input.title).trim(),description:String(input.description).trim(),sender,target,status:'OPEN',priority:'NORMAL'});await TicketMessage.create({ticketId:ticket._id,author:{id:sender.id,name:sender.name,role:sender.role,type:'USER'},content:ticket.description});await TicketActivity.create({ticketId:ticket._id,actor:{id:sender.id,name:sender.name,type:'USER'},action:'CREATED'});await enqueueEvent(eventKey[ticket.type],this.eventPayload(ticket,actor));return ticket;}
 async list(actor:Actor,q:any){const page=Math.max(1,Number(q.page)||1),limit=Math.min(50,Math.max(1,Number(q.limit)||20));const f:any=actor.type==='USER'?{'sender.id':actor.id}:{};for(const k of ['type','status'])if(q[k])f[k]=q[k];if(q.targetType)f['target.type']=q.targetType;if(q.search){const v=String(q.search).replace(/[.*+?^${}()|[\]\\]/g,'\\$&');f.$or=[{title:{$regex:v,$options:'i'}},{description:{$regex:v,$options:'i'}}];}const date:any={};if(q.from)date.$gte=new Date(q.from);if(q.to)date.$lte=new Date(`${q.to}T23:59:59.999Z`);if(Object.keys(date).length)f.createdAt=date;const[items,total]=await Promise.all([Ticket.find(f).sort({lastActivityAt:-1}).skip((page-1)*limit).limit(limit).lean(),Ticket.countDocuments(f)]);return{items,total,page,limit,totalPages:Math.ceil(total/limit)};}
 async detail(actor:Actor,id:string){if(!Types.ObjectId.isValid(id))throw new Error('Ticket không hợp lệ.');const f:any={_id:id};if(actor.type==='USER')f['sender.id']=actor.id;const ticket=await Ticket.findOne(f).lean();if(!ticket)throw new Error('Ticket không tồn tại.');const mf:any={ticketId:id};if(actor.type==='USER')mf.internal=false;const[messages,activities,attachments]=await Promise.all([TicketMessage.find(mf).sort({createdAt:1}).lean(),TicketActivity.find({ticketId:id}).sort({createdAt:1}).lean(),TicketAttachment.find({ticketId:id}).select('-objectKey').lean()]);await Ticket.updateOne({_id:id},{$set:{[actor.type==='USER'?'userUnread':'adminUnread']:false}});return{...ticket,messages,activities:actor.type==='ADMIN'?activities:[],attachments};}
 async message(actor:Actor,id:string,input:any){const ticket:any=await Ticket.findOne({_id:id,...(actor.type==='USER'?{'sender.id':actor.id}:{})});if(!ticket)throw new Error('Ticket không tồn tại.');if(ticket.status==='CLOSED')throw new Error('Ticket đã đóng.');const content=String(input.content||'').trim();const attachmentIds=Array.isArray(input.attachmentIds)?[...new Set(input.attachmentIds.map(String))]:[];if(attachmentIds.length>5)throw new Error('Mỗi phản hồi chỉ được tối đa 5 tệp.');if(attachmentIds.length){const owned=await TicketAttachment.countDocuments({_id:{$in:attachmentIds},ticketId:id,ownerId:actor.id,messageId:null});if(owned!==attachmentIds.length)throw new Error('Tệp đính kèm không hợp lệ hoặc không thuộc người gửi.');}if(!content&&!attachmentIds.length)throw new Error('Nội dung hoặc tệp đính kèm là bắt buộc.');const identity=await this.snapshot(actor);const internal=actor.type==='ADMIN'&&Boolean(input.internal);const message=await TicketMessage.create({ticketId:id,author:{id:identity.id,name:identity.name,role:identity.role,type:actor.type},content,internal,attachmentIds});if(attachmentIds.length)await TicketAttachment.updateMany({_id:{$in:attachmentIds},ticketId:id,ownerId:actor.id,messageId:null},{$set:{messageId:message._id}});const old=ticket.status;if(actor.type==='USER'&&old==='RESOLVED')ticket.status='OPEN';ticket.lastActivityAt=new Date();if(!internal)ticket[actor.type==='USER'?'adminUnread':'userUnread']=true;await ticket.save();await TicketActivity.create({ticketId:id,actor:{id:identity.id,name:identity.name,type:actor.type},action:internal?'INTERNAL_NOTE':'REPLIED',fromValue:old,toValue:ticket.status});if(!internal)await enqueueEvent(actor.type==='USER'?RoutingKey.INBOX_USER_REPLIED:RoutingKey.INBOX_ADMIN_REPLIED,this.eventPayload(ticket,actor,{summary:content.slice(0,240)}));return message;}
 async status(actor:Actor,id:string,status:string){if(!TICKET_STATUSES.includes(status as any))throw new Error('Trạng thái không hợp lệ.');const ticket:any=await Ticket.findById(id);if(!ticket)throw new Error('Ticket không tồn tại.');const old=ticket.status;ticket.status=status;ticket.lastActivityAt=new Date();ticket.userUnread=true;await ticket.save();await TicketActivity.create({ticketId:id,actor:{id:actor.id,name:actor.name,type:'ADMIN'},action:'STATUS_CHANGED',fromValue:old,toValue:status});await enqueueEvent(RoutingKey.INBOX_STATUS_CHANGED,this.eventPayload(ticket,actor));return ticket;}
 async upload(actor:Actor,id:string,files:Express.Multer.File[]){const ticket=await Ticket.findOne({_id:id,...(actor.type==='USER'?{'sender.id':actor.id}:{})});if(!ticket)throw new Error('Ticket không tồn tại.');if(ticket.status==='CLOSED')throw new Error('Ticket đã đóng.');if(!files.length||files.length>5)throw new Error('Chọn từ 1 đến 5 tệp.');const created=[];for(const file of files){const stored=await storeFile(id,file);created.push(await TicketAttachment.create({ticketId:id,ownerId:actor.id,originalName:file.originalname,mimeType:stored.mime,sizeBytes:file.size,objectKey:stored.key}));}return created.map(({_id,originalName,mimeType,sizeBytes})=>({_id,originalName,mimeType,sizeBytes}));}
 async attachment(actor:Actor,id:string){const row:any=await TicketAttachment.findById(id);if(!row)throw new Error('Tệp không tồn tại.');const ticket:any=await Ticket.findById(row.ticketId);if(!ticket||(actor.type==='USER'&&ticket.sender.id!==actor.id))throw new Error('Bạn không có quyền truy cập tệp.');return{row,stream:await getFile(row.objectKey)};}
}
export default new TicketService();
